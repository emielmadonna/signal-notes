{
  "R3b": [
    {
      "file": "components/add-document/add-document-sheet.tsx",
      "signature": "const insertQuery = supabase .from(\"documents\") .insert({ org_id: orgId, title: title.trim(), kind, body, ext: \"TXT\", size_bytes: sizeBytes, added_by: user.id, ",
      "reason": "Deferred-query pattern: the builder is assigned so abortSignal(signal) can be applied conditionally, then awaited and destructured in the NEXT statement, where `insertError` is checked and surfaced via internalError(). The error IS handled; it simply cannot appear in the same statement as the .insert() call."
    },
    {
      "file": "components/add-document/add-document-sheet.tsx",
      "signature": "const auditQuery = supabase.from(\"audit_events\").insert({ org_id: orgId, document_id: doc.id, event: \"UPLOADED\", detail: `Pasted text \u00b7 ${sizeBytes} bytes`, act",
      "reason": "Same deferred-query pattern as the documents insert above: `auditError` is destructured and surfaced two statements later. The error IS handled."
    }
  ],
  "R3c": [
    {
      "file": "app/signin/signin-form.tsx",
      "signature": "// Unparseable value: fall through to the safe default.",
      "reason": "safeNextPath's URL parse: an unparseable ?next= value is not an error condition to report, it is untrusted input to reject. The backstop is the explicit `return \"/\"` on the next line, which is also what a same-origin failure returns."
    },
    {
      "file": "components/add-document/add-document-sheet.tsx",
      "signature": "// Non-JSON body: the status-based message below is the backstop (R3).",
      "reason": "response.json() on an error response: a server that answered non-2xx with a non-JSON body (proxy error page, empty body) is expected. Backstop: the status-code-derived message built immediately below, so the user still sees a truthful failure."
    },
    {
      "file": "components/add-document/upload-client.ts",
      "signature": "// Body was not JSON (proxy page, empty body). Fall through to the // status-based message below \u2014 the status line is the backstop.",
      "reason": "Identical to the add-document sheet's parse guard: a non-JSON error body falls through to the status-line message, which is the stated backstop."
    },
    {
      "file": "components/theme-provider.tsx",
      "signature": "// localStorage can be unavailable (private mode); the cookie still // persists the choice, so this is safe to ignore.",
      "reason": "localStorage write in private/blocked-storage modes throws. Backstop: the theme cookie, which is the authoritative persistence and is written on the same path, so the choice survives without localStorage."
    },
    {
      "file": "components/theme-provider.tsx",
      "signature": "// Unavailable storage means nothing to restore; keep the server theme.",
      "reason": "localStorage READ in the same blocked-storage case. Backstop: the server-rendered theme from the cookie is already applied and is left in place, which is the correct fallback."
    },
    {
      "file": "lib/supabase/server.ts",
      "signature": "// setAll was called from a Server Component, where Next.js forbids // writing cookies. Safe to ignore: middleware.ts refreshes the // session cookies on every ",
      "reason": "@supabase/ssr's documented pattern: cookie writes from a Server Component throw by design in Next. Backstop stated in the comment and real \u2014 middleware.ts refreshes session cookies on every matched request."
    },
    {
      "file": "lib/use-generation-stream.ts",
      "signature": "// A malformed / partial frame is skipped, never shown as an event.",
      "reason": "SSE frame parse: a partial frame at a chunk boundary is normal streaming, not a fault. Backstop: the frame is skipped and the next complete frame is parsed; the terminal 'done' message is what settles the stream."
    },
    {
      "file": "lib/use-generation-stream.ts",
      "signature": "// reader already released (aborted mid-read); nothing to do.",
      "reason": "reader.cancel() on an already-released reader during abort. Backstop: none needed \u2014 the intended post-condition (the reader is not consuming) already holds, which is why the call was made."
    },
    {
      "file": "lib/use-generation-stream.ts",
      "signature": "// Non-JSON error body; keep the status-code message.",
      "reason": "Same non-JSON error-body case as the upload client. Backstop: the status-code message is kept and shown."
    }
  ]
}
